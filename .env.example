# MongoDB Connection String
# Get this from MongoDB Atlas (we'll set this up together)
MONGODB_URI=mongodb+srv://your-username:your-password@cluster.mongodb.net/brahmastra-club?retryWrites=true&w=majority

# JWT Secret Key (Change this to something secure)
JWT_SECRET=brahmastra-super-secret-key-change-this-in-production

# Port (Railway/Render will set this automatically)
PORT=5000

# Node Environment
NODE_ENV=production
